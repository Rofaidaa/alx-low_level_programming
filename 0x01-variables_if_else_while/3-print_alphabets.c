#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, and then in uppercase.
 *
 * Return: Always 0.
 */
int main(void)
{
char letter;
char capital;
letter = 'a';
capital = 'A'
while
(letter <= 'z') {
putchar(letter);
letter++;
}
while
(capital <= 'Z') {
putchar(capital);
capital++;
}
putchar('\n');
return (0);
}
